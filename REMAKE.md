# Routy 정책 변경 및 다음 작업 인계

최초 작성: 2026-09-10  
최종 갱신: 2026-09-11

## 0. 이 문서의 용도와 현재 상태

정책 논의에서 확정된 변경, 폐기된 안과 남은 구현 결정을 보존하는 인계 문서다. 대화 원문 전체가 아닌 개발 재개에 필요한 결정 기록이다.

- 2026-09-11까지 확정한 제품 방향과 카카오 DevTalk 답변은 기존 docs와 AGENTS.md에 반영했다.
- 아래의 '확정'은 제품 정책 합의다. 카카오 좌표의 일시 사용·즉시 폐기 조건은 제공자 답변까지 확인했지만 기능 구현이나 테스트가 완료됐다는 뜻은 아니다.
- 이 문서는 기존 AGENTS.md의 작업 승인·보안·테스트·경계 규칙을 대체하지 않는다. 정책 충돌은 해당 문서 변경 작업으로 해결한다.
- 다음 작업에서는 이 파일과 적용되는 AGENTS.md를 읽고, 로드맵의 현재 상태와 실제 Git 변경을 확인한 뒤 진행한다.

## 1. 가장 최근에 확정한 방향

1. 국내 여행 서비스로 제한한다. 일정마다 자동차 또는 대중교통 중 하나의 이동수단을 선택한다.
2. 지도·장소 검색은 카카오를 사용한다. 지도 관련 API는 무료 범위만 사용하고 한도 전에 차단한다. AI는 유료 사용한다.
3. 카카오 지도와 좌표는 일정 제작 중 사용한다.
4. 완성된 일정은 고정된 HTML 템플릿에 저장된 시간표, 사용자 작성 일정 항목 이름, 카카오맵 외부 링크를 채워 표시한다. 일정마다 HTML 파일을 새로 생성하거나 저장하지 않는다. 완성 화면과 공유 화면에서 지도를 표시하거나 카카오 API를 새로 호출하지 않는다.
5. 카카오 응답의 장소명을 자동 저장하지 않는다. 사용자가 각 항목의 '일정에 표시할 이름'을 직접 입력한다.
6. 카카오 장소명을 입력창 기본값이나 placeholder로 자동 삽입하지 않는다. 제작 중 검색 결과의 장소명 표시는 가능하다는 전제로 논의했다.
7. 영속 저장 대상은 사용자 작성 이름, 카카오 placeId/placeUrl, 방문 날짜·순서·시각·체류시간 등 자체 일정 정보다.
8. 카카오 주소·좌표·카테고리·전화번호·검색 원문·경로 원문·경로 좌표는 영속 저장하지 않는다. 좌표와 검색 결과는 한 번의 제작 흐름과 서버 요청에서만 일시적으로 사용하고 즉시 폐기한다.
9. '전체 일정 다시 배치' 기능은 삭제한다. 다시 배치하려면 도시 선택부터 새 일정을 만든다. 새 일정 생성 때문에 기존 일정을 자동 삭제하지 않는다.
10. 완료 후 좌표를 복원해 자동 최적화하는 흐름은 채택하지 않는다. 완료 후에는 일정 제목, 표시 이름과 메모만 수정한다.

## 2. 프로젝트와 하네스 작업 이력

### 목적

- Spring Boot 기반 AI 여행 일정 추천 및 경로 최적화 프로젝트다.
- 기능 완성과 함께 Spring 아키텍처·백엔드 설계를 학습하는 취업 준비용 프로젝트다.
- 당장은 사용자와 지인 중심으로 사용하려고 한다. 소규모 사용이 외부 약관 예외를 뜻하지는 않는다.
- Controller/Service/Repository 책임, Entity/DTO 분리, 순수 Java 경로 알고리즘, 비밀키 보호 원칙은 유지한다.

### 프론트 경로와 이전 작업

- 사용자가 Routy2를 Routy로 직접 변경했다.
- 실제 서비스 프론트 경로는 `src/main/resources/static/Routy/`다. CSS, JS, img 등 역할별 폴더 정리 방향이다.
- 이전 대화에서는 README 및 로드맵·경계표의 Routy 경로 정리, Routy AGENTS.md 축약, 루트 AGENTS.md와 docs 인덱스의 선택적 읽기 규칙 보완을 진행했다고 기록되어 있다.
- 이 파일 작성 전 실제 git 상태에는 다수의 사용자 변경이 존재한다. Routy 삭제/이동/신규 파일이 staged와 unstaged로 혼재한다. 루트 AGENTS.md, README.md, docs/00-docs-index.md, 템플릿 AGENTS.md, toMake.md 등에도 기존 변경이 있다.
- REMAKE.md는 작업 시작 시 빈 파일로 이미 staged 상태였다. 이번 작성은 working tree에만 추가하며 staging/commit은 하지 않는다.
- 과거 수정 보고와 현재 git diff는 동일하지 않을 수 있다. 다음 작업에서 변경 대상의 실제 상태를 다시 확인한다.
- Routy/INTEGRATION.md의 Routy2 표기 잔존은 앞선 검사에서 발견한 항목이다. 수정 시 현재 내용 재확인이 필요하다.

### 하네스 목표

- 모든 코드·모든 문서를 매 작업마다 읽지 않는다.
- 루트와 대상 폴더 AGENTS.md, 경계표의 해당 행, 현재 로드맵 작업, 대상 코드와 직접 의존 계약만 우선 읽는다.
- 필요할 때 관련 문서의 절·endpoint·ADR, 다른 도메인의 공개 Service/DTO만 추가로 확인한다.
- 작업 ID, Allowed Paths, Conditional Paths, Forbidden Paths, 읽은 기준, 검증 방법을 설명한다.
- 무관한 리팩터링·파일 이동·포맷 변경을 섞지 않는다. 기존 사용자 변경을 보존한다.
- 과거 '1~8번 점검' 중 뒤쪽 항목은 정책 논의 때문에 중단되었다. 현재 기록만으로 원래 번호별 세부 제목을 모두 복원할 수 없으므로 임의로 번호에 내용을 붙이지 않는다. 아래 주제별 후속 목록을 기준으로 재개한다.

## 3. 지역 선택과 AI

### 서비스 범위

- 국내 한 기준 지역을 선택해 여행을 만든다. 해외·국가 선택 흐름은 폐기한다.
- 제주를 별도 서비스 정책으로 취급하지 않는다. 국내 지역에 동일 기준을 적용한다.
- 선택 지역까지 찾아가는 교통편은 서비스 범위 밖이다.
- 내 위치 기반 추천은 넣지 않는다.

### 지역 직접 검색

- 사용자는 서울·광역시·도 가운데 상위 지역을 먼저 고른 뒤 그 아래의 최종 시·군·구 하나를 선택한다.
- 세종특별자치시는 하위 시·군·구 없이 그 자체를 최종 지역으로 선택한다. 읍·면·동은 지원하지 않는다.
- 직접 선택과 AI 추천은 공공 행정구역 자료로 만든 같은 `regions.json`을 사용한다.
- 관광지는 주소가 최종 선택 지역에 속하거나 그 지역 대표 좌표 반경 20km 이내인 경우 허용한다.

### AI 추천

- 사용자가 AI 추천을 선택한 경우에만 원하는 여행을 자연어로 입력한다. 예: '바다가 예쁜 곳에 가고 싶어'.
- AI가 내부 JSON의 지역 설명을 참조하여 가장 적합한 지역 3개와 이유를 반환한다.
- JSON 밖의 지역을 AI가 새로 만들어 추천하지 않는다.
- 별도의 필수 여행 성격 체크리스트나 전역 취향 입력 단계는 두지 않는다. AI 추천 자연어 요청과 별개의 취향 프로필을 만들지 않는다.
- AI 추천에서 빈 입력을 기본 추천으로 처리하는 흐름은 채택하지 않았다.
- 지역 설명은 DB 대신 JSON으로 관리한다. `src/main/resources/data/regions.json`은 후보 위치이며 실제 생성·스키마 확정은 아직이다.
- 여기서 RAG는 제공한 지역 자료를 참고한다는 의미다. 벡터 DB, 임베딩, 특정 프레임워크 도입을 승인한 것이 아니다.
- AI 역할은 지역 추천·이유와 음식 자연어에서 메뉴 후보를 구조화하는 것이다. 동선 최적화·시간 배치·숙소 탐색 중심·음식점 이탈거리 계산은 서버가 담당한다.

## 4. 제작 흐름

최종 시·군·구 선택 → 날짜·활동 시간 입력 → 관광지와 체류시간 선택 → Haversine 추정 일정 → 숙소 지도 선택(숙박 여행) → 음식 자연어 입력·메뉴 확정 → 음식점 지도 선택 → 실제 경로 검증 → 최종 검토·저장 → 텍스트 시간표와 링크 조회.

- 관광지 선택 중에는 잔여 시간을 보여주며, 최종 날짜와 순서는 동선 결정 때 배치한다.
- 숙소 선택 후 출발/종료 조건을 반영하여 일정이 실제로 들어가는지 다시 검증해야 한다.
- 장소 선택 직후 일정 후보에 넣기 전에 사용자가 빈 입력창에 표시 이름을 작성한다.

## 5. 관광지 검색과 선택 제약

- 카카오 장소 검색으로 10~15개 정도를 지도와 목록에 표시한다. 충분한 결과가 없는데 개수를 채우려고 장소를 만들어내지 않는다.
- 목록/마커 선택을 연동하고 카카오 상세 링크를 제공한다.
- 관광지 검색과 목록 추천에 AI를 쓰지 않는다.
- 최초 검색 반경은 20km다. 지도를 이동한 뒤 '이 지역 재검색'으로 다시 조회할 수 있다.
- 기준 행정구역 내부 장소는 중심에서 20km보다 멀어도 허용한다. 다른 행정구역의 장소는 기준 지역 중심에서 20km 이내인 경우 허용한다.
- 하루 최대 관광지 5개, 전체 관광지 수는 여행 일수 × 5 이하다. 식당·숙소를 이 관광지 5개에 포함시키는 의미가 아니다.
- 영업시간·휴무·예약 가능 여부를 보장하지 않는다. 사용자가 외부 상세 정보로 확인하도록 안내한다.
- 카카오 분류는 내부 기본 체류시간 계산에만 사용한다. 유형은 노출·저장하지 않고 사용자는 추천 체류시간만 10분 단위로 조정한다.

## 6. 시간 예산과 체류시간

- 날짜별 기본 활동 시작/종료 시간, 첫날 도착 시간, 마지막 날 종료 시간을 받아 실제 활동 창을 계산한다.
- 활동 창이 점심·저녁 시간대와 겹치면 식사 60분과 한쪽 이동 여유 기본 15분을 일정 후보에 반영한다.
- 관광지 선택 단계에서는 체류시간과 Haversine 예상 이동시간으로 날짜별 수용 가능성을 검사한다.
- 추가 선택으로 예산이 음수가 되면 선택을 막는다. 전체 합계뿐 아니라 각 날짜에 들어갈 수 있는지도 검사한다.
- 최종 날짜 배치 전에는 일별 시간 창에 나누어 넣을 수 있는지 검사하는 방식이며, 선택 즉시 날짜를 고정하는 것은 아니다.
- 최종 계산에서는 인접 구간의 실제 예상 이동시간을 사용하며 식사 여유와 이동시간을 중복 합산하지 않는다.

| 유형 | 기본 체류시간 |
|---|---:|
| GENERAL 일반 | 90분 |
| MUSEUM 박물관/전시 | 120분 |
| NATURE 자연 | 90분 |
| EXPERIENCE 체험 | 180분 |
| THEME_PARK 놀이공원 | 360분 |
| HIKING 등산 | 240분 |

- 사용자는 10분 단위로 최소 30분~최대 8시간까지 조정할 수 있다.
- 최종 저장에는 확정한 체류시간 숫자만 두고 기본값·사용자 수정 여부를 나타내는 별도 enum은 저장하지 않는다.
- 외부 카테고리의 영속 저장과 자체 체류시간 값의 저장은 구분한다.

## 7. 거리와 배치 알고리즘

- 좌표 간 Haversine 직선거리 계산은 서버 내부에서 수행한다. 거리 계산 자체에 지도 API 추가 호출은 필요하지 않다.
- MVP 예상 도로거리 = 직선거리 × 보정계수 1.3.
- 평균속도는 60km/h, 예상 분 = ceil(직선거리 km × 1.3)로 계산한다. 실제 주행시간이 아닌 추정값이다.
- 사용자는 검증 부담 때문에 우선 위 수치로 진행하기로 했다. 지역별 교통 검증 완료로 표시하지 않는다. 계수·속도는 조정 가능하게 설계한다.
- 방문 순서: Nearest Neighbor + 2-opt. 순수 Java로 유지하고 AI에 의존하지 않는다.
- 날짜별 시간 창, 관광지 수, 체류시간, 식사, 이동거리와 지리적 가까움을 고려하여 배치한다.
- 알고리즘이 실패하면 '현재 조건에서 일정을 만들지 못했습니다'라고 안내한다. 실제로 가능한 일정이 없음을 증명한 것으로 표현하지 않는다.
- 관광지를 자동 삭제하거나 체류시간을 임의로 줄이지 않는다. 사용자가 조건을 조정한다.
- 최초 제작 중에는 다른 순서·날짜 조합을 시도할 수 있으나, 제시된 일정을 사용자 모르게 계속 바꾸지 않는다.

### 실제 경로 검증

- 자동차는 카카오모빌리티, 대중교통은 카카오맵 경로 API를 사용한다. 일정 하나에서는 이동수단 하나만 선택한다.
- 거의 완성된 일정의 인접 구간만 조회하며 제공자별 요청 제한은 구현 시 공식 문서와 앱 콘솔에서 다시 확인한다.
- 식당·숙소도 실제 경로 지점에 포함한다.
- 페리 회피 옵션을 사용하려는 방향이다. 실제 옵션 이름/지원 여부는 API 문서로 확인한다.
- 경로 없음은 '자동차 경로를 확인하지 못했습니다'로 안내하며 실제 이동 불가능이라고 단정하지 않는다.
- 사용자 경로 한도나 제공자 안전 한도를 사전 확보하지 못하면 외부 경로를 한 번도 호출하지 않고 전체 일정을 Haversine 추정시간으로 반환하며 경고를 표시한다.
- 과거 '완성된 일정의 일차 탭을 열 때 실제 경로 호출·저장'은 최신 완료 화면 정책으로 폐기되었다.
- 실제 경로 검증 뒤 활동 종료 시각을 넘으면 저장을 차단한다. 완료 후에는 경로를 재호출하거나 원문을 보관하지 않는다.

## 8. 숙소

- 당일치기는 숙소가 없다. 첫 관광지에서 시작하여 마지막 일정 장소에서 끝난다.
- 1박 이상은 숙소 선택 필수이며 한 여행에 숙소 하나만 허용한다.
- 첫날: 별도의 출발 숙소 없이 관광지부터 시작하고 숙소에서 종료한다.
- 중간 날: 숙소에서 시작하고 숙소에서 끝난다.
- 마지막 날: 숙소에서 시작하고 마지막 일정 장소에서 종료한다. 숙소로 돌아가지 않는다.
- 선택 관광지 좌표의 기하 중앙값을 숙소 검색 중심으로 사용한다.
- 기본 반경 5km에서 지도와 목록을 보여주고 사용자가 넓히면 10km까지 검색한다.
- 숙소 품질을 알 수 없는 거리 점수로 순위를 매기지 않는다. 사용자가 가격·시설·후기 등 외부 상세 정보와 지도 위치를 직접 비교한다.
- 10km에서도 마음에 드는 숙소가 없으면 `관광지 위치로 검색`을 눌러 메도이드 관광지 주변을 탐색할 수 있다.
- 지도 이동 뒤 현재 지도 영역 재검색도 지원한다. 숙소가 정해지면 각 날짜의 시작·종료점에 반영해 일정을 다시 검사한다.

## 9. 식사와 음식점

- 사용자는 `이 관광지에서 유명한 음식을 먹고 싶어`처럼 원하는 음식을 자연어로 입력할 수 있다.
- AI는 선택 지역과 관광지 맥락을 함께 보고 메뉴·검색어·추천 이유·선택적 기준 관광지를 1~5개 제안한다. 사용자가 확정하거나 수정한 값만 검색에 사용한다.
- 메뉴에 맞는 음식점이 없다고 다른 메뉴로 자동 대체하지 않는다.
- 맛집 평점·리뷰를 가진 것처럼 AI가 판단하지 않는다. 카카오 검색 결과만으로 맛집 품질을 보장하지 않는다.
- 관광지 날짜/순서를 먼저 정하고 estimate 뒤 주변 음식점을 끼워 넣는다. 음식점 때문에 관광지 상대 순서를 바꾸지 않는다.
- 동선 중간 지점보다 식사 시각에 대응하는 관광지 주변을 우선 탐색한다.
- 후보는 식사 전후 관광지에서 생기는 Haversine 이탈이 작은 순서로 목록에 보여주고 지도 마커와 연동한다. 사용자는 현재 지도 영역도 재검색할 수 있다.
- 사용자가 음식점을 선택하지 않으면 해당 식사 장소는 미정으로 둘 수 있다. 선택한 음식점만 최종 일정에 저장하고 실제 경로 검증에 포함한다.

### 식사 시간

- 점심 시작 범위: 11:30~14:00, 선호 시각 12:00.
- 저녁 시작 범위: 17:30~20:30, 선호 시각 18:00.
- 기본 식사시간 60분.
- 관광지 이동 사이에 식사가 필요한 경우 식사 60분과 식사 한쪽 이동 여유 기본 15분씩, 합계 30분을 고려한다.
- 음식점 확정 시 기존 A→B 이동을 A→식당→B로 교체한다. 기존 이동과 새 이동을 모두 더하지 않는다.
- 실제 추정 우회시간과 식사 이동 여유는 중복 계산하지 않는다.
- 음식점 삽입이 시간 창에 맞지 않으면 가까운 식당, 시간 조정, 관광지 제거 등 필요한 조정을 사용자가 선택한다.
- 놀이공원 등 장기 체류가 식사 시간과 60분 이상 겹치면 내부 식사 처리 가능 방향이다. 내부 식사는 관광지 체류시간 안에 포함하고 별도 식사 예약을 해제해 이중 차감을 방지한다.
- 외부 식사를 택하면 나갔다 돌아오는 이동도 고려한다. 내부 식사 가능성을 API가 보장한다고 가정하지 않는다.

## 10. 이미지와 지도 화면

- 초기에 실제 사진, 네이버 이미지 검색 API, 기본 이미지 등을 논의했다.
- 이후 '지도 적극 활용'으로 확정하면서 장소 사진 조회 기능을 MVP에서 제외했다. 카카오 장소 검색 응답에 사진이 자동 포함되는 것으로 가정하지 않는다.
- TourAPI 이미지 도입은 대안 제안이었으며 채택하지 않았다.
- 제작 화면: 지도/목록/마커로 선택을 돕는다.
- 완료 및 공유 화면: 사용자 작성 제목·시간표·카카오 링크로 표시한다.
- 사용자 최종 확정: 고정된 HTML 템플릿을 재사용하고, 조회한 일정의 이름·링크·시간표 데이터를 정해진 위치에 채운다. 일정마다 새 HTML 파일을 생성하거나 보관하지 않는다.
- 서버에는 구조화된 일정 데이터를 저장한다. 템플릿에 데이터를 넣는 구체 방식(브라우저 JavaScript 또는 서버 템플릿)은 아직 정하지 않았다. HTML 다운로드 기능도 별도 요구사항이 아니다.

## 11. 저장 정책과 데이터 수명

### 최종 채택: 사용자 작성 표시 이름

예: 사용자가 '궁궐 산책', '점심 식사', '바닷가 구경'을 직접 입력하고, 해당 항목 옆에 카카오맵 URL을 연결한다.

- `displayName`은 사용자 입력이며 카카오 `place_name` 자동 복사값이 아니다.
- 카카오 장소명 자동 입력 후 확인 버튼만 받는 방식은 채택하지 않는다.
- 카카오 응답 전체를 일정 저장 DTO에 넘기지 않는다.
- 기록 후보: 일정/항목 ID, 사용자 소유권, 일차·날짜·순서, 시작/종료 시각, 체류시간, 사용자 작성 표시 이름, 카카오 ID/URL, 사용자 메모.
- 항목 타입(관광/숙박/식사)은 서비스 자체 개념으로 설계하되 외부 카테고리 그대로의 영속 저장과 구분한다.
- 정확한 명칭·null 정책·길이·검증·관계는 현재 `docs/03-database.md`와 `docs/04-api-spec.md`를 따른다.
- URL은 허용 도메인·형식을 검증하고 사용자 입력은 HTML 이스케이프 등 기본 출력 처리를 한다.

### 영속 저장 제외

- 카카오 장소명, 주소, 좌표, 전화번호, 카테고리, 검색 원문.
- 카카오 경로 원문·폴리라인·지도 이미지.
- Caffeine/Redis/DB에 카카오 장소검색 결과를 캐시하는 과거 안.
- 브라우저 localStorage/IndexedDB에 외부 결과를 남겨 저장 제한을 우회하는 안도 승인하지 않았다.

### 적용 경계

- 2026-09-11 DevTalk 답변은 한 번의 제작 흐름에서 일시적으로 저장·참조하고 즉시 폐기하는 구조에 적용한다. 이를 임의의 장기 임시 저장이나 여러 흐름 간 재사용 허용으로 확대 해석하지 않는다.
- 카카오 좌표와 검색 결과는 브라우저 탭 메모리에만 유지하고 estimate·create 요청마다 필요한 값을 전달한다. 서버는 각 요청 처리 뒤 폐기한다.
- 지역 기준 좌표·이름과 AI 설명은 카카오 검색 결과가 아니라 출처가 확인된 정적 지역 데이터로 관리한다.
- API 키/개인정보/원문 응답은 로그·fixture·Git·오류 응답에 남기지 않는다.

## 12. 완료 후 변경과 공유

### 최신 확정

- 전체 재배치는 삭제한다.
- 다시 배치하려면 도시 선택부터 별도 새 일정 생성 흐름으로 간다.
- 완료된 일정의 지도·경로·장소 상세 재조회는 하지 않는다.
- 완료·공유 화면은 저장된 사용자 작성 일정 정보와 카카오 외부 링크를 보여준다.

### 폐기된 과거 편집안

- 이전에는 일정 변경 전용 화면에서 날짜 이동·순서·체류시간·식사 변경 및 일차별 최적화를 허용하고, 저장 전 draft를 유지하기로 했다.
- 이후 좌표 폐기와 새 일정 생성 정책으로 바뀌었다. 완료된 일정에서는 일정 제목, 사용자 표시 이름과 메모만 수정한다.
- 제작 완료 전 단계 이동/수정은 완료 후 전체 재배치와 구분한다.

### 공유 합의

- 일정은 기본 비공개다.
- 소유자가 공유를 켜면 추측하기 어려운 토큰 링크를 발급한다.
- 비회원은 읽기 전용으로 볼 수 있다. 편집/재배치 권한은 없다.
- 공유 해제 시 토큰을 무효화하고 재활성화 때 새 토큰을 만든다.
- 소유자 이메일은 공유 화면에 노출하지 않는다.
- 공유 화면에서 새로운 AI·장소·경로 API 호출은 없다.
- 과거 '공유 화면에서 마지막 저장 경로 표시'는 최신 경로 미저장 정책으로 폐기한다.

## 13. 인증과 비용 제한

- 이메일/비밀번호 + Spring Security/JWT 방향이다. 소셜 로그인은 MVP 제외다.
- 본인 일정만 조회·수정·삭제할 수 있다. 공유 토큰 조회는 별도 읽기 계약이다.
- 비회원 범위는 메인/설명/샘플과 공유 일정이다.
- AI 추천, 지역/장소 검색, 일정 생성·저장 등 실제 기능은 로그인 필요로 합의했다.
- 순수 알고리즘은 먼저 개발할 수 있으나 TravelPlan 저장/Controller 전에 인증·소유권 구조를 준비한다. 기존 '인증은 핵심 MVP 이후' 순서는 수정 대상이다.
- 이후 지인 전용 운영을 언급했지만 위 인증 정책을 취소하지는 않았다. 초대 가입/운영자 승인 정책은 정하지 않았다.

### 대화에서 정한 초기 호출 제한

| 기능 | 사용자 분당 | 사용자 일당 |
|---|---:|---:|
| 장소 검색 | 20 | 300 |
| 자동차 경로 | 60 | 120 |
| 대중교통 경로 | 60 | 120 |
| AI 지역 추천 | 2 | 10 |
| AI 메뉴 분석 | 3 | 15 |

- AI는 유료 사용한다. '모든 외부 API는 무료만'이라고 쓰지 않는다.
- 지도 관련 API는 무료 한도 내에서만 사용한다. 무료 사용 가능 앱 조건 확인, 유료 추가 사용 비활성화, 서비스 차단 장치를 함께 둔다.
- API별 일간/공통 월간 한도 중 먼저 도달하는 한도의 90%에서 신규 호출 차단 방향이다.
- 분할 길찾기 요청 각각을 집계한다. 동시 요청 예약/집계와 재시도도 한도에 포함하도록 설계한다.
- 일반 사용자 제한은 429와 재시도 정보를 반환한다. 최종 경로 검증 쿼터가 부족할 때는 429 대신 외부 호출 없이 전체 Haversine fallback 일정과 경고를 반환한다.
- 지도 Web SDK는 브라우저에서 호출하므로 서버 카운터만으로 완전 통제된다고 보장하지 않는다. 콘솔 사용량·도메인 제한·지도 로딩 정책이 필요하다.
- '캐시 적중이면 외부 호출 한도를 차감하지 않는다'는 일반 원칙은 가능하지만, 카카오 장소 결과 캐시는 최신 정책에서 제외했다.
- 모든 일일 한도는 `Asia/Seoul` 자정에 초기화하고 사용자·기능·`requestId`로 중복 호출을 막는다. 전체 AI 예산은 운영 설정에서 별도로 확정한다.
- 제공자의 수치/무료 조건은 변할 수 있다. 예전 답변의 쿼터 수치를 영구 상수로 신뢰하지 말고 적용 시 공식 문서와 앱 콘솔을 확인한다.

## 14. 폐기하거나 채택하지 않은 안

| 과거 안 | 최신 처리 |
|---|---|
| 국가 입력 → 해외 도시 추천 | 국내 지역 직접 검색 / JSON 기반 AI 추천 |
| Google Places/Routes 중심 | 카카오 중심 |
| 대중교통 선택 | 자동차만 |
| 모든 지역 설명 DB 구축 | 추천 설명 JSON |
| TourAPI를 주요 장소 저장 원천으로 전환 | 대안 검토 후 미채택 |
| 내부 행정구역 검색으로 카카오 지역 검색 대체 | 미채택 |
| 네이버 지도/이미지 검색으로 전환 | 미채택 |
| 관광지·숙소·음식점 실제 이미지 API | 지도/목록 중심으로 제외 |
| 장소명·주소·좌표 스냅샷 영속 저장 | 사용자 작성 이름 + ID/URL + 자체 일정 정보 |
| 카카오 장소명만 자동 저장하고 위험 감수 | 사용자 작성 이름 방식으로 대체 |
| 카카오 검색 응답 Caffeine/Redis 캐시 | 제외 |
| 완성 일차 탭 클릭 때 길찾기 호출 | 완료 화면 외부 호출 없음 |
| 마지막 실제 도로 경로를 DB에 저장해 재조회 | 제외 |
| 전체 일정 다시 배치 버튼 | 삭제, 도시 선택부터 새 일정 생성 |
| 완료 후 장소명 재검색으로 좌표 복원 | 새 일정 생성 방향으로 대체 |
| 관광지 30일/숙소 14일/식당 7일 재검증 | 저장 원천·완료 후 재조회 변경으로 적용 보류/재설계 |
| 식당 주변으로 관광지 재배치 | 관광지 순서 고정 후 식사 삽입 |
| 식당을 일정에 저장하지 않음 | 식사 항목도 자체 일정에 포함 |
| 산술평균 기반 숙소 추천 점수 | 기하 중앙값·메도이드 기반 지도 탐색, 숙소 순위 없음 |
| 제주 별도 정책 | 국내 공통 정책 |
| 내 위치 추천 | 제외 |
| AI도 무료만 | AI는 유료 |

## 15. 외부 자료와 해석 주의

아래는 대화에서 확인/인용했던 공식 자료의 재확인용 링크다. 링크가 있다는 이유만으로 모든 설계가 승인되었다는 뜻은 아니다. 법적 확약이 아니라 구현 전 확인 근거다.

- 카카오 Local 데이터 저장 질의: https://devtalk.kakao.com/t/local-api/151430
  - 대화에서는 공식 운영진이 id/place_url 저장을 허용하고 기타 데이터 임시 저장을 허용하지 않는 취지로 답했다고 확인했다. 원문과 현재 적용 범위를 다시 확인할 수 있다.
- 사용자 선택 정보 저장 관련 추가 질의: https://devtalk.kakao.com/t/topic/151514
  - 당시 미해결 쟁점을 허용 근거로 사용하지 않는다.
- 카카오맵 REST API: https://developers.kakao.com/docs/ko/kakaomap/rest-api
- 카카오 무료/유료 쿼터: https://developers.kakao.com/docs/ko/getting-started/quota
- 카카오내비 개요: https://developers.kakao.com/docs/ko/kakaonavi/common
- 카카오모빌리티 개발자 문서: https://developers.kakaomobility.com/
- 네이버 현행 API 약관: https://developers.naver.com/products/terms/
  - 네이버 장소명만 별도 저장하는 예외를 확인하지 못했다. 네이버로 바꾸면 저장 문제가 해결된다고 결론 내리지 않았다.
- 네이버 API HUB 이관: https://developers.naver.com/notice/article/32530
- 네이버 지역검색 최대 5건 공지: https://developers.naver.com/notice/article/7528
- TourAPI 국문 관광정보 서비스: https://www.data.go.kr/tcs/dss/selectApiDataDetailView.do?publicDataPk=15101578
  - 당시 무료/이용허락범위 제한 없음과 사진의 개별 조건을 확인한 대안이다. 채택하지 않았다.

이전 답변에서 카카오 길찾기 결과의 영속 저장을 허용된 것처럼 제안한 부분도 별도 검증되지 않았다. 최신 정책은 결과를 영속 저장하지 않으므로 그 제안을 복구하지 않는다. 대중교통 API 지원 여부 등 과거 답변도 현재 제공 기능과 다를 수 있으나, 자동차만이라는 제품 결정은 유지한다.

## 16. 문서 정렬 결과

아래 문서는 현재 확정 정책에 맞춰 정렬했다. 과거 Google 중심 기록은 `docs/06-decisions.md`에서 폐기된 결정의 이력으로만 남긴다.

| 대상 | 주요 변경/검사 항목 |
|---|---|
| docs/01-requirements.md | 국내 범위, 지역 선택, JSON AI, 최대 관광지 5, 시간 예산, 숙소/식사, 완료 화면, 최소 저장, 재배치 삭제 |
| docs/03-database.md | 카카오 ID·URL, 사용자 작성 표시 이름, 일정 필드, 식사 항목, 외부 응답 미저장 |
| docs/04-api-spec.md | 카카오 장소·경로 endpoint와 DTO, 제작 상태/최종 저장 분리, 사용자 입력 검증, 당일치기, 완료 후 외부 호출 제거 |
| docs/06-decisions.md | Google·글로벌 등 과거 결정을 Superseded로 보존하고 최신 카카오·국내 정책 ADR 추가 |
| docs/07-implementation-readiness.md | 확정/미확정/구현됨 분리, 인증 순서, 외부 API/저장 제약, 제작 상태 공백 |
| docs/08-test-strategy.md | 예산 경계·날짜별 배치·식사 중복 차감·호텔 끝점·5개 제한·AI JSON 외 후보 방지·최소 저장·쿼터·공유 권한 |
| docs/09-operations.md | 카카오/유료 AI, 키·쿼터·재시도·원문 미기록·임시 상태 폐기·캐시 제거 |
| docs/10-definition-of-done.md | 새 요구사항과 검증 기준, 완료 조회 외부 호출 없음, 저장 필드 점검 |
| docs/11-command-roadmap.md | 카카오 단계, 인증 선행, 문서 확정 후 구현 순서, 재배치 삭제 |
| docs/12-harness-boundaries.md | 도메인 책임·허용 경로·카카오/AI 역할·인증 순서·선택적 읽기 |
| 루트 및 관련 하위 AGENTS.md | 카카오·국내 전제, 책임 범위, 해당 폴더 참조 문서와 금지 경로, 프론트 지도 규칙 |
| docs/00-docs-index.md, README.md | 새 문서 탐색 경로·제품 설명·현재 프론트 경로 |
| Routy/INTEGRATION.md 및 Routy/AGENTS.md | Routy2 잔존, 제작 지도 vs 완료 텍스트, 새 프론트 연동 계약 |

- 문서 정렬 완료는 기능 구현이나 테스트 완료를 뜻하지 않는다.

## 17. 다음 작업에서 확인할 세부 계약

현재 활성 작업은 없고 실제 기능 구현도 시작하지 않았다. 다음 작업은 로드맵의 한 작업 ID와 Change Envelope로 시작한다.

- F0에서 실제 dependency·DB 테스트 방식·profile 기준을 확인한다.
- U1에서 JWT 수명·재발급·폐기와 `requestId` 성공 응답 재현 계약을 확정한다.
- G1에서 공공 행정구역 데이터의 구체 출처·갱신 절차를 확정한다.
- R1에서 이동수단별 Haversine 보정계수와 기하 중앙값 수렴·동률 규칙을 확정한다.
- C1/R2에서 자동차·대중교통 공식 endpoint, 앱별 쿼터, timeout과 오류 매핑을 구현 직전 재확인한다.
- AI 모델·전체 예산과 공유 토큰·idempotency의 세부 보안 계약을 해당 작업에서 확정한다.

## 18. 이번 인계 문서 작업 기록

- Task ID: DOC-REMAKE-HANDOFF.
- Allowed Paths: REMAKE.md.
- Conditional Paths: 없음.
- Forbidden Paths: 기존 docs/AGENTS.md, 애플리케이션 코드, 테스트, 설정, migration, 정적 자산.
- 읽은 기준: 루트 AGENTS.md, docs/12-harness-boundaries.md, 대화의 확정/폐기 이력, REMAKE.md 기존 상태와 diff.
- 이번 작업은 정책 인계 기록이며 실제 계약·기능 구현을 변경하지 않는다.
- 검증: UTF-8 내용 확인, 변경 범위 및 공백 오류 검사. 문서만 작성하므로 Gradle 테스트는 생략한다.
- 기존 staged 변경 보존. git add/commit/stash/reset 실행 안 함.

다음 작업 시작 요청 예: 'REMAKE.md와 로드맵을 읽고 현재 Git 상태를 확인해. 구현은 한 작업 ID와 Change Envelope 범위로 진행하자.'

## 19. 2026-09-11 추가 설계 결정

이 절은 2026-09-10 인계 이후 사용자와 추가로 확정한 내용이다. 현재 관련 요구사항·DB·API·ADR·테스트·운영·로드맵 문서에도 반영했다.

### 이동수단과 경로 계산

- 서비스는 일정마다 `자동차` 또는 `대중교통` 중 하나를 선택할 수 있게 한다.
- 한 일정 안에서 자동차와 대중교통을 혼합하지 않는다.
- 자동차 실제 경로는 카카오모빌리티 자동차 길찾기 API, 대중교통 실제 경로는 카카오맵 대중교통 경로 API를 사용한다.
- 장소를 검색하거나 선택할 때마다 실제 길찾기를 호출하지 않는다.
- 선택 장소 좌표로 Haversine 직선거리와 서버의 결정적 알고리즘을 이용해 방문 순서와 시간표 후보를 먼저 만든다.
- 거의 완성된 후보 하나가 정해진 뒤 인접 구간만 실제 경로 API로 검증한다.
- 장소·식사·순서가 바뀌면 영향을 받는 구간만 다시 조회한다. 이동수단이 바뀌면 전체 인접 구간을 다시 조회한다.
- 실제 경로 API의 소요 시간은 10분 단위로 올림한다. 예를 들어 37분은 40분, 45분은 50분으로 사용한다.
- 원본 초·분 값과 경로 응답은 영속 저장하지 않고 최종 10분 단위 예상 이동시간만 자체 일정 정보로 저장하는 방향이다. 제공자 정책 확인 상태는 아래 별도 절을 따른다.
- 10분 단위 올림 외에 별도의 고정 이동 여유시간을 자동으로 더하지 않는다.
- 완료 화면에서는 모든 값을 `예상 이동시간`으로 표시하며 정확한 도착 시간을 보장하는 표현을 사용하지 않는다.

### 실제 경로 조회 실패와 시간 초과

- 타임아웃·5xx 같은 일시적 기술 오류는 1회만 재시도한다. 재시도도 외부 호출 한도에 포함한다.
- 재시도 후에도 기술적으로 실패하거나 무료 한도에 도달한 경우에는 좌표 기반 추정시간을 10분 단위로 올려 사용할 수 있다.
- 제공자가 정상 응답으로 `경로 없음`을 반환하거나 좌표·요청값이 유효하지 않으면 추정값으로 대체하지 않고 최종 확정을 차단한다.
- 실제 경로 검증 후 하루의 체류·식사·이동시간 합계가 사용자 입력 종료 시각을 넘으면 최종 확정을 차단한다. 별도의 초과 허용 오차는 두지 않는다.
- 서버는 장소를 자동 삭제하거나 체류시간을 임의로 줄이지 않는다.
- 사용자는 종료 시각 변경, 체류시간 조정, 관광지 제거, 음식점 선택 변경·해제, 장소·순서 변경 중 필요한 방법을 직접 선택한다.
- 종료 시각이나 체류시간만 바꾸면 경로를 재호출하지 않는다. 장소 제거, 음식점 선택 변경·해제 또는 순서 변경은 새로 연결되거나 바뀐 구간만 다시 조회한다.
- 시간이 남아도 서버가 장소를 자동으로 추가하지 않는다.

### 장소 표시 이름과 체류시간

- 사용자가 카카오 검색 결과에서 장소를 선택한 직후, 일정 후보에 추가하기 전에 `일정에 표시할 이름`을 빈 입력창에 직접 작성한다.
- 카카오 장소명을 입력값이나 placeholder로 자동 삽입하지 않는다.
- 표시 이름은 앞뒤 공백 제거 후 필수이며 1~50자로 검증한다. 공백만 있는 값과 길이 초과 값은 거부한다.
- 동일한 표시 이름은 허용하고 별도 금칙어 필터는 두지 않는다. 출력할 때 HTML 이스케이프 등 기본 보안 처리를 적용한다.
- 카카오 장소 카테고리는 제작 중 기본 체류시간을 계산하는 데만 사용한다.
- 체류 유형은 사용자에게 노출하지 않고 사용자가 선택하거나 변경하게 하지 않는다.
- 카카오 카테고리 원문과 내부 체류 유형은 영속 저장하지 않는다.
- 화면에는 추천 체류시간만 표시하며 사용자가 10분 단위로 조정할 수 있다.
- 체류시간은 최소 30분, 최대 8시간이다. 분류가 불명확한 관광지는 기본 90분을 사용한다.
- 식당은 별도 식사시간 정책인 60분을 적용하고 숙소는 관광지 체류시간 대상에서 제외한다.
- 최종 저장에는 사용자가 확정한 `stayMinutes`만 포함하고 체류 유형은 포함하지 않는다.

### 완료 후 편집

- 완료된 일정에서는 일정 제목, 각 항목의 사용자 작성 표시 이름, 사용자 메모만 수정할 수 있다.
- 방문 날짜·순서·시각·체류시간·이동수단은 완료 후 수정할 수 없다.
- 장소나 동선을 바꾸려면 기존 일정을 보존한 채 새 일정을 생성한다.
- 완료 후에는 카카오 API를 호출하거나 경로를 다시 계산하지 않는다.

### 국내 지역 기준 데이터

- 국내 광역자치단체와 모든 시·군·구를 지원하고 읍·면·동은 지역 선택 단위에서 제외한다.
- 지역 목록은 DB Entity가 아니라 `src/main/resources/data/regions.json` 정적 기준 데이터로 관리하는 방향을 확정했다.
- 서버 시작 시 지역 기준 데이터를 메모리에 로딩하며 직접 지역 검색과 AI 지역 추천이 같은 목록을 사용한다.
- 각 지역은 내부 `regionId`, 공식 이름, 짧은 이름, 별칭, 상위 광역자치단체, 지역 유형, 대표 좌표, 데이터 기준일·출처를 가진다.
- 대표 좌표는 카카오 검색 결과에서 영구 저장하지 않고 출처가 명확한 공공 행정구역 데이터로 구축한다.
- 광역자치단체는 탐색을 위한 상위 항목이며 최종 여행 지역으로 저장하지 않는다. 최종 선택은 시·군·구 하나이고 세종특별자치시만 예외로 그 자체를 선택한다.
- 완성 일정에는 `regionId`와 당시의 표준 표시 이름을 저장한다.

### 제작 중 상태와 중복 요청

- 현재 채택한 구현 후보는 카카오 검색 결과를 브라우저 탭의 JavaScript 메모리에서만 사용하고 새로고침·완료·취소·탭 종료 때 폐기하는 방식이다.
- 카카오 좌표·검색 결과를 DB, Redis, Caffeine, 서버 세션, `localStorage`, `sessionStorage`, IndexedDB에 저장하지 않는다.
- 서버는 좌표를 거리 계산·방문 순서 최적화·길찾기를 수행하는 요청 범위에서만 사용하고 요청 종료 후 보관하지 않는 방향이다.
- 이 방식은 2026-09-11 카카오 DevTalk의 "일시적으로 저장 및 참조 후 즉시 폐기하는 구조는 허용 가능합니다."라는 답변을 근거로 확정했다. 정책 확인은 구현 완료를 뜻하지 않는다.
- 외부 API와 AI 요청에는 사용자·기능·`requestId` 조합의 중복 요청 방지를 적용한다.
- `requestId`와 처리 상태는 여러 서버 인스턴스가 공유하도록 MySQL `request_executions`에 10분 동안 저장한다. 요청·응답 원문과 좌표는 저장하지 않는다.
- 처리 중인 같은 요청은 외부 제공자를 다시 호출하지 않는다. 외부 호출이 이미 실행된 요청은 이후 실패해도 사용량에 포함한다.
- 만료된 중복 요청 기록은 정리 작업으로 삭제한다. 서버가 재시작돼도 10분 수명 안의 기록은 유지된다.

### 메뉴 분석 AI 한도와 실패 처리

- 메뉴 분석 AI는 사용자당 분당 3회, 하루 15회로 제한한다.
- 일일 한도는 `Asia/Seoul` 자정에 초기화한다.
- 잘못된 구조화 응답에 대한 내부 재시도는 최대 1회이며 재시도도 한도에 포함한다.
- 같은 `requestId`의 중복 클릭이나 네트워크 재전송은 새로운 AI 호출로 처리하지 않는다.
- 한도 초과는 HTTP 429와 재시도 가능 정보를 반환하는 방향이다.
- 메뉴 요청 문장은 최대 200자이며 AI는 선택 지역·관광지 맥락을 바탕으로 메뉴·식당 검색어·추천 이유·선택적 기준 관광지를 1~5개 구조화한다.
- AI는 음식점 자체를 추천·평가하지 않는다. 자연어 요청이 `이 지역의 유명한 음식`처럼 열린 요청이면 맥락에 맞는 구체 메뉴를 제안할 수 있으며 사용자가 확정하거나 수정한다.
- 재시도 후에도 실패하면 사용자가 식당 검색어를 직접 입력할 수 있게 하고 기존 일정 작성 내용은 유지한다.
- AI 요청·응답 원문은 DB·일반 로그·오류 응답에 저장하지 않는다.

## 20. 카카오 이용정책 확인 상태

### 공식 자료에서 확인한 내용

- 카카오맵 Local API의 장소 ID와 URL은 저장·활용 가능한 범위라는 최근 담당자 답변이 있다.
- 좌표·주소·전화번호·카테고리·검색 응답 원문 등은 별도 저장하지 않고 live call로만 사용해야 한다는 담당자 답변이 있다.
- 영구 장소 DB 목적이 아니고 작업 종료 후 삭제하더라도 서버 DB 등에 임시 저장해 활용하는 방식은 허용되지 않는다는 최근 답변이 있다.
- 장소 ID만으로 장소 상세를 다시 단건 조회하는 Local API는 현재 제공되지 않는다.
- 네이버 지역 검색 API도 검색 결과 저장·캐시·가공 제한이 있어 카카오를 네이버로 단순 교체해 같은 문제를 해결할 수 없다고 확인했다. 네이버 Maps 공개 API 목록에는 자동차 Directions는 있지만 대중교통 경로 API는 없다.
- TourAPI 등 공공데이터는 저장·가공 가능한 관광정보 원천이지만 카카오맵의 모든 관광지·숙소·음식점·생활 POI와 동일한 범위를 제공하지 않는다.

### 카카오 문의 결과와 확정 조건

2026-09-11 카카오 DevTalk에서 "일시적으로 저장 및 참조 후 즉시 폐기하는 구조는 허용 가능합니다."라는 답변을 받았다. 다음 구조를 허용된 사용 범위로 확정한다.

1. 사용자가 한 브라우저 화면에서 여러 장소를 검색·선택하는 동안 선택 좌표를 JavaScript 메모리에만 유지한다.
2. 새로고침 복구 없이 동일한 제작 흐름에서 좌표를 서버의 단일 일정 생성 요청에 전달한다.
3. 서버는 그 요청 안에서만 직선거리 계산, 방문 순서 최적화, 자동차·대중교통 길찾기 요청에 좌표를 사용한다.
4. 요청 처리와 일정 제작이 끝나면 좌표와 Local API 응답을 폐기한다.
5. 좌표·주소·전화번호·카테고리·카카오 장소명·검색 원문은 영속 저장하지 않는다.
6. 완성 일정에는 장소 ID·URL과 사용자가 빈 입력창에 직접 작성한 표시 이름, 자체 일정 정보만 저장한다.

좌표와 Local API 응답은 이 범위를 넘어 영속 저장·캐시·로그 기록하거나 완료 일정에서 재사용하지 않는다. 제공자 정책이나 답변 조건이 바뀌면 좌표 기반 신규 제작을 중지하고 계약을 다시 검토한다.

## 21. 2026-09-11 인계 갱신 작업 기록

- Task ID: DOC-REMAKE-HANDOFF-UPDATE.
- Allowed Paths: `REMAKE.md`.
- Conditional Paths: 없음.
- Forbidden Paths: 기존 docs/AGENTS.md, 애플리케이션 코드, 테스트, 설정, migration, 정적 자산.
- 읽은 기준: 루트 `AGENTS.md`, `docs/AGENTS.md`, `docs/11-command-roadmap.md`, `docs/12-harness-boundaries.md`, `REMAKE.md`, 2026-09-11 대화에서 확정한 설계와 확인한 제공자 자료.
- 이번 작업은 인계 기록 갱신이며 요구사항·DB·API 계약이나 실제 기능을 변경하지 않는다.
- 카카오 임시 사용 정책 확인은 완료됐으며, 실제 기능은 구현·테스트 전까지 완료로 표시하지 않는다.
