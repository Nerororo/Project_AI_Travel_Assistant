spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/travel?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:traveller}
    password: ${DB_PASSWORD}

  jpa:
    show-sql: true
