# Routy | 포트폴리오 · 자소서 · 면접 정리

> 이 문서는 프로젝트 계약이 아니라 프로젝트를 통해 고민하고 배운 내용을 정리하는 개인 기록이다.
> 구현 전 설계와 실제 구현 결과를 구분한다.

## 작성 원칙

- **구현 완료**: 코드·테스트·실행 결과로 확인 가능한 사실만 쓴다.
- **설계 완료**: 요구사항·아키텍처·ADR로 정했지만 아직 코드가 없는 내용이다.
- **구현 예정**: 로드맵에만 있는 작업이며 완료 경험처럼 표현하지 않는다.
- 성능, 테스트 수, 정확도와 비용 절감은 실제 측정 뒤에만 기록한다.
- 제공자 정책 준수 여부는 담당자 답변과 공식 근거 없이 성과로 주장하지 않는다.

## 1. 프로젝트 시작 이유

나는 여행할 때 관광지, 음식점과 숙소를 미리 정하고 1~2시간 단위로 일정을 표로 정리하는 편이다. 장소를 각각 조사한 뒤 이동 동선까지 고려해 배치하는 과정에는 시간이 많이 든다. 이 과정을 줄이고 보기 좋은 일정표까지 만들 수 있는 서비스가 있으면 좋겠다는 생각에서 Routy를 시작했다.

Routy는 사용자가 국내 지역, 여행 기간, 이동수단과 방문 장소를 고르면 체류·식사·이동 시간을 조합해 실행 가능한 여행 계획을 만드는 서비스다. 지역을 정하지 못한 사용자는 AI로부터 국내 지역 후보와 추천 이유를 받을 수 있다.

### 한 줄 소개

**AI의 자연어 후보 생성과 서버의 결정적인 일정·경로 계산을 분리하고, 외부 장소 데이터의 수명을 통제하는 Spring Boot 여행 일정 프로젝트**

## 2. 현재 상태와 목표 범위

| 구분 | 현재 확인된 상태 | 포트폴리오 표현 |
|---|---|---|
| 애플리케이션 | Java 21·Spring Boot 골격과 `GET /hello` 코드가 존재한다. | 실행 환경 감사 전에는 코드 기준선까지만 사실로 쓴다. |
| DB 기반 | Docker Compose와 datasource 설정 초안은 있으나 JPA·MySQL·Flyway dependency와 migration은 없다. | DB 연결·migration 완료로 쓰지 않는다. |
| 설계 | 국내·카카오 방향의 요구사항, API, DB, 테스트, 운영, ADR, 로드맵과 하네스가 정렬돼 있다. | 설계·정책 게이트 완료와 기능 구현 완료를 구분해 표현한다. |
| 핵심 도메인 | User, Region, AI, Place, Route, Recommendation, TravelPlan 코드는 구현 전이다. | 구현 경험으로 쓰지 않는다. |
| 외부 연동 | OpenAI·카카오 Client는 구현 전이다. | 장애 처리·비용 절감 경험으로 쓰지 않는다. |
| 화면 | Routy 정적 시안은 있으나 새 API 흐름과 인증에 연결되지 않았다. | 완성된 프런트엔드로 표현하지 않는다. |
| 정책 | 2026-09-11 DevTalk 답변으로 좌표의 일시 저장·참조 후 즉시 폐기 구조를 확정했다. | 정책 확인과 구현 완료를 구분하고, 실제 폐기 동작은 구현·테스트로 증명한다. |

### 목표 사용자 흐름

```text
회원가입·로그인
→ 서울·광역시·세종 또는 도·특별자치도 아래 시·군 직접 선택, 또는 AI 지역 후보 선택
→ CAR 또는 PUBLIC_TRANSIT 선택
→ 관광지 선택과 숙소 지도 탐색
→ 사용자 장소 이름과 체류 시간 입력
→ Haversine 기반 일정 추정
→ AI 메뉴 제안 확정과 식사 슬롯별 음식점 지도 탐색
→ 최종 인접 구간의 실제 경로 검증
→ 시간 초과 확인 후 완료 일정 저장
→ 외부 호출 없는 조회·제한 편집·공유
```

## 3. 핵심 구현 계획

| 영역 | 구현 목표 | 보여줄 역량 |
|---|---|---|
| User | JWT 인증, 일정 소유권과 사용자별 호출 한도 | Spring Security, 인증·인가, 보안 테스트 |
| Region | 자체 선택하는 서울·광역시·세종, 상위 도·특별자치도와 최종 시·군, 검색 필터용 구·군 기준 데이터 적재·검색 및 AI 허용 목록 | 기준 데이터 검증, Bean, 결정적 검색 |
| Place | 카카오 검색, 체류 기본값, `selectionToken`, 최소 저장 계약 | Client 경계, 데이터 수명, 입력 무결성 |
| Route | Haversine·Nearest Neighbor·2-opt와 이동수단별 실제 경로 검증 | 순수 알고리즘, 결정성, 실패 모델 |
| TravelPlan | 추정·완료 계산 분리와 완료 Aggregate 원자 저장 | Service 조합, Transaction, JPA |
| Recommendation | 음식점의 시간 적합성과 Haversine 동선 이탈 평가 | 정책 분리, 점수와 동률 정렬 |
| AI | 허용 지역 3개와 지역·관광지 맥락의 메뉴 1~5개 구조화 | LLM 출력 검증, 사용자 확정, 오류·한도 관리 |
| 운영 | profile, migration, health, 한도와 Fake 기반 테스트 | 환경 분리, 보안, 관찰성 |
| Frontend | 작성 상태와 완료·공유 화면 연결 | 비동기 상태, 데이터 폐기, 접근성 |

## 4. 설계 이유와 고민

### 4.1 AI와 서버의 책임을 분리한 이유

AI에게 일정 전체를 맡기면 이동 거리, 순서와 시간 수용 가능 여부를 같은 입력에서 재현하고 검증하기 어렵다. AI는 국내 지역 후보의 이유와 지역·관광지 맥락의 메뉴 제안처럼 자연어 처리에 적합한 일을 맡고, 서버는 허용 목록 검증, 거리, 순서, 시간표, 음식점 이탈 순위와 저장을 담당하도록 설계했다.

**면접에서 강조할 점**

- LLM 출력은 구조화된 후보이며 사실이나 최적해가 아니다.
- 지역 ID는 서버가 제공한 `regions.json` 허용 목록으로 검증한다.
- 핵심 일정 규칙은 결정적인 Java 코드와 자동 테스트로 검증한다.

### 4.2 도메인 중심 패키지 구조

Routy는 사용자, 지역, 장소, 경로, 일정, 추천과 AI처럼 변경 이유가 다른 기능으로 구성된다. 기술 계층만으로 묶는 대신 각 도메인 아래 필요한 Controller·Service·Repository·DTO를 두고, 다른 도메인은 공개 Service와 전달 DTO로만 사용한다.

### 4.3 계층을 나눈 이유

```text
Controller : HTTP 요청·응답과 DTO validation
Service    : use case와 비즈니스 규칙, Transaction 경계
Repository : 같은 도메인의 Entity 조회·저장
DTO        : API 또는 도메인 간 데이터 전달
Entity     : 영속 상태와 Aggregate 관계
Client     : 외부 제공자 통신
Algorithm  : 프레임워크에 의존하지 않는 계산
```

HTTP, DB와 외부 API 형식이 바뀌어도 일정 규칙이 함께 흔들리지 않도록 변경 이유를 분리한 구조다.

### 4.4 외부 API를 Client와 Fake로 분리한 이유

OpenAI와 카카오 API는 네트워크, 비용, 쿼터와 장애라는 불확실성이 있다. `AiClient`, `KakaoPlaceClient`, 자동차·대중교통 `RouteClient`를 분리하면 자동 테스트에서 실제 호출 없이 성공, timeout, 5xx, 잘못된 응답과 경로 없음을 재현할 수 있다.

### 4.5 장소 데이터를 최소 저장하는 이유

외부 장소 데이터를 DB에 복제하면 최신성뿐 아니라 제공자 정책 문제가 생긴다. 완료 일정에는 카카오 장소 ID·외부 URL과 사용자 작성 이름·메모·체류 시간만 저장한다. 카카오 장소명, 주소, 좌표, 전화번호, 카테고리와 원문 응답은 저장하지 않는다.

완료 화면은 외부 제공자에 재조회하지 않고 저장된 사용자 정보와 시간표를 표시한다. 카카오 장소 상세는 저장한 외부 링크로 이동한다.

2026-09-11 카카오 DevTalk에서 일시적으로 저장·참조한 뒤 즉시 폐기하는 구조가 허용된다는 답변을 받았다. 이를 근거로 작성 중 좌표를 브라우저 메모리와 한 번의 서버 계산에서만 사용하고 즉시 폐기하는 계약을 확정했다. 실제 준수 여부는 구현과 테스트가 끝난 뒤 판단한다.

### 4.6 2단계 경로 계산을 선택한 이유

모든 후보 조합에 실제 경로 API를 호출하면 호출량과 비용이 커진다. 먼저 Haversine 거리로 Nearest Neighbor와 2-opt를 수행하고, 거의 완성된 후보의 인접 구간만 실제 경로로 확인한다.

실제 시간은 10분 단위로 올린다. 일정 종료 시각을 넘으면 서버가 장소를 삭제하거나 체류 시간을 줄이지 않고 저장을 막아 사용자가 조정하게 한다. 필요한 경로 쿼터를 사전 확보하지 못하면 외부 호출 없이 일정 전체를 Haversine 예상시간으로 계산하고 warning을 반환한다. 호출 중 기술 장애는 한 번 재시도한 뒤 일정 전체를 추정값으로 대체할 수 있지만 정상적인 경로 없음은 실패로 처리한다. 일부 구간만 실제 경로이고 나머지만 추정인 결과는 만들지 않는다.

### 4.7 순수 Java 알고리즘

`route/algorithm`은 Spring Bean, JPA Repository, HTTP·AI Client에 의존하지 않는다. 처음부터 최적해를 주장하지 않고 Nearest Neighbor를 기준선으로 삼은 뒤 2-opt 적용 전후 거리와 실행 시간을 실제 측정한다.

### 4.8 외부 호출 뒤 트랜잭션을 시작하는 이유

일정 생성에는 장소 선택 검증, 경로 조회와 시간 계산이 필요하다. 트랜잭션을 먼저 열면 네트워크 대기 동안 DB 연결을 점유한다. 외부 호출과 계산을 끝낸 뒤 짧은 트랜잭션으로 TravelPlan·Day·Item·PlanPlace 전체를 저장해 부분 저장을 막도록 설계했다.

### 4.9 Flyway와 `ddl-auto: validate`

JPA 자동 schema 변경 대신 첫 schema부터 Flyway versioned migration으로 관리하고 Hibernate는 일치 여부만 검증할 계획이다. DB 변경 이력과 UNIQUE·CHECK·FK를 실제 DB 테스트로 재현하기 위한 선택이다.

### 4.10 인증을 먼저 구현하는 이유

AI·카카오 호출에는 사용자별 한도가 필요하고 일정에는 처음부터 소유권이 필요하다. 인증을 나중에 넣으면 API와 migration을 다시 바꿔야 하므로 개발 기반 직후 User·Security·JWT를 구현하는 순서로 바꿨다.

### 4.11 완료 일정을 제한적으로 편집하는 이유

완료 뒤 날짜·순서·시각·체류 시간·이동수단을 바꾸면 실제 경로와 시간표를 다시 검증해야 한다. 완료 일정은 제목, 사용자 장소 이름과 메모만 고칠 수 있게 하고 동선을 바꾸려면 새 일정을 만들도록 범위를 줄였다.

### 4.12 프런트엔드 방향

`static/Routy`의 정적 시안을 새 API 흐름으로 연결한다. 프런트 작업은 백엔드 전체가 끝날 때까지 미루지 않고 U1·A1·P1·S1·T1의 선행 조건을 충족한 W1 작업부터 하나씩 연결한다. `P1-06`에서는 시각 UI보다 먼저 브라우저 메모리 한정 장소 상태 모듈을 만들고, 작성 중 데이터와 서버가 확정한 완료 상태를 분리한다. 완료·공유 화면은 지도 없이 고정 HTML에 저장된 시간표와 카카오 링크만 표시한다.

**구현 후 확인할 점**

- 뒤로 이동하거나 오류가 나도 안전한 입력이 유지되는가?
- 중복 제출과 오래된 검색 응답을 막았는가?
- 카카오 임시 데이터가 브라우저 저장소에 남지 않는가?
- 추정과 실제 검증 결과를 사용자가 구분할 수 있는가?
- 시간 초과·경로 없음·한도 초과·인증 만료를 다르게 안내하는가?
- 모바일·데스크톱과 키보드 흐름을 검증했는가?

## 5. AI 사용과 안전장치

| 기능 | AI 역할 | 서버 역할 |
|---|---|---|
| 지역 추천 | 허용 목록에서 지역 ID 3개와 이유 생성 | 목록·개수·중복·필수 필드 검증 |
| 메뉴 분석 | 메뉴 1~5개·카카오 검색어·이유·대상 관광지 제안 | 사용자 확정 검증, 음식점 검색과 동선 순위 |
| 추천 이유 | 짧은 자연어 설명 생성 | 길이·형식 검증 |

AI에 맡기지 않는 항목:

- 장소 ID·좌표 생성 또는 추측
- 장소 존재 검증
- 거리·이동 시간과 방문 순서
- 숙소 검색 중심·지도 탐색과 음식점 이탈 점수·최종 선택
- DB 저장과 일정 수용 가능 여부

구현 후 확인할 질문:

- 허용 목록 밖 지역 ID를 어떻게 차단했는가?
- 구조화 응답 위반과 provider 장애를 어떻게 구분했는가?
- 지역 AI 2회/분·10회/일, 메뉴 AI 3회/분·15회/일을 어떻게 적용했는가?
- 사용자 원문과 provider 원문을 저장·로그하지 않는지 어떻게 검증했는가?

## 6. 기술 스택과 상태

| 기술 | 상태 | 선택 이유 |
|---|---|---|
| Java 21 | 구성됨 | 정적 타입과 Spring 생태계 학습 |
| Spring Boot 4.1.1 | 기본 골격 | 웹 요청 처리와 DI 기반 계층 구조 |
| Gradle | 구성됨 | dependency·빌드·테스트 관리 |
| MySQL 8.4·Docker Compose | 설정 초안 | 재현 가능한 로컬 DB 환경 |
| Spring Data JPA | 도입 예정 | Aggregate 영속성과 Repository 학습 |
| Flyway | 도입 예정 | versioned schema와 DB 제약 관리 |
| Spring Security·JWT | 도입 예정 | 인증·인가와 일정 소유권 |
| 카카오 Local | 연동 예정·임시 사용 조건 확인 | 국내 장소 검색 |
| 카카오모빌리티 | 연동 예정·임시 사용 조건 확인 | 자동차 경로 |
| 카카오맵 대중교통 | REST 계약 확인·연동 예정 | 대중교통 예상 경로, 서비스 일일 900건 차단 |
| OpenAI API | 연동 예정 | 지역 후보와 메뉴·검색어·이유·대상 관광지 구조화 |
| JUnit·Fake·Mock | 확장 예정 | 외부 호출 없는 자동 테스트 |
| HTML·CSS·JavaScript | 정적 시안·API 미연동 | 단계별 W1 연결, 브라우저 메모리 수명과 접근성 검증 |

## 7. 자소서 문장 초안

### 현재 사실에 기반한 문장

> 여행 계획에서 장소 탐색과 이동 동선 설계에 반복적으로 시간이 드는 문제를 해결하고자 Spring Boot 여행 일정 서비스 Routy를 기획했습니다. 구현 전에 국내 지역 기준, AI와 서버의 책임, 외부 장소 데이터 수명, 일정 계산과 저장 경계를 요구사항·API·DB·ADR로 분리해 설계하고 있습니다.

> AI 결과를 일정의 정답으로 사용하지 않고 지역·메뉴 후보 생성에 제한했습니다. 거리, 방문 순서, 시간 예산과 추천 점수는 서버의 결정적인 규칙으로 두어 테스트 가능한 구조를 설계했습니다.

### 구현 뒤 보강할 문장 틀

> 카카오 장소·경로 연동에서 [실제 문제]를 해결하기 위해 [구현]을 적용했고, [테스트·측정 결과]로 [영향]을 확인했습니다.

> TravelPlan 생성 중 [실패 사례]를 고려해 외부 호출 뒤 원자 저장을 적용했고, [통합 테스트 결과]로 부분 저장이 발생하지 않음을 검증했습니다.

대괄호는 구현·테스트·측정 뒤에만 채운다.

## 8. 예상 면접 질문과 답변 초안

### Q. 왜 AI에게 일정을 전부 맡기지 않았나요?

AI는 자연어 후보와 설명에는 적합하지만 거리, 순서, 시간 용량과 저장 규칙은 재현성과 검증이 중요합니다. AI는 허용된 지역 후보와 메뉴 검색어만 만들고 서버가 결정적인 계산을 담당하도록 나눴습니다.

### Q. 왜 도메인 중심 패키지인가요?

사용자, 지역, 장소, 경로, 일정, 추천과 AI는 변화 이유가 다릅니다. 도메인 안에 필요한 계층을 두고 공개 Service·DTO로 협력하면 관련 코드를 찾기 쉽고 내부 구현 결합을 줄일 수 있습니다.

### Q. Fake Client의 장점은 무엇인가요?

자동 테스트가 네트워크·키·비용·제공자 상태에 의존하지 않습니다. timeout, 5xx, 계약 위반, 정상 빈 결과와 경로 없음도 고정 입력으로 재현할 수 있습니다.

### Q. 왜 실제 경로를 모든 후보에 호출하지 않나요?

초기 후보 생성은 Haversine으로 충분하고 실제 경로 호출은 비용과 한도가 있습니다. Nearest Neighbor와 2-opt로 후보를 좁힌 뒤 최종 인접 구간만 조회해 호출량을 통제합니다. 효과는 구현 뒤 실제 호출 수로 측정해야 합니다.

### Q. 왜 외부 호출 전에 트랜잭션을 열지 않나요?

외부 호출은 지연되거나 실패할 수 있습니다. 계산을 먼저 끝내고 짧은 트랜잭션으로 완료 Aggregate만 저장하면 DB 연결 점유와 부분 저장 위험을 줄일 수 있습니다.

### Q. 장소 정보를 모두 저장하지 않는 이유는 무엇인가요?

외부 데이터의 최신성과 제공자 정책을 직접 관리하지 않기 위해서입니다. 카카오 ID·URL과 사용자 작성 정보만 저장하고 좌표·주소·카테고리·제공자 장소명은 저장하지 않습니다. 작성 중 좌표는 2026-09-11 DevTalk 답변에 따라 한 번의 제작 흐름에서만 일시적으로 사용하고 즉시 폐기합니다.

### Q. 완료 일정의 동선을 왜 수정할 수 없나요?

동선을 바꾸면 실제 경로와 시간 예산을 다시 검증해야 합니다. 완료 데이터의 의미를 단순하게 유지하기 위해 텍스트만 수정하고 지리적 변경은 새 일정으로 만들도록 설계했습니다.

## 9. 구현 후 성과 기록

| 날짜 | 작업 ID | 문제 | 해결 방식 | 검증 결과 | 자소서·면접 키워드 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

기록할 수 있는 근거:

- 단위·Service·API·DB·브라우저 테스트 결과
- migration 적용과 DB 제약 검증
- Fake로 timeout·5xx·계약 위반·경로 없음을 재현한 사례
- Nearest Neighbor와 2-opt의 실제 거리·실행 시간 비교
- 추정과 실제 경로 검증 전후의 10분 단위 시간표 변화
- 외부 호출 수, retry·fallback·한도 차단 측정
- JPA query와 N+1 확인
- 설계 변경의 ADR과 트레이드오프

## 10. 참고 문서

- 요구사항: `docs/01-requirements.md`
- 아키텍처: `docs/02-architecture.md`
- DB·API: `docs/03-database.md`, `docs/04-api-spec.md`
- 개발 방향·ADR: `docs/05-development-plan.md`, `docs/06-decisions.md`
- 현재 상태: `docs/07-implementation-readiness.md`
- 테스트·운영·완료: `docs/08-test-strategy.md`, `docs/09-operations.md`, `docs/10-definition-of-done.md`
- 작업 순서·경계: `docs/11-command-roadmap.md`, `docs/12-harness-boundaries.md`
