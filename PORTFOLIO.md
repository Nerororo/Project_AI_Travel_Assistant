# Routy | 포트폴리오 · 자소서 · 면접 정리

> 이 문서는 프로젝트 계약 문서가 아니라, 프로젝트를 통해 무엇을 고민하고 배웠는지 정리하는 개인 기록이다.
> 구현 전 설계와 실제 구현 결과를 구분해 작성한다.

## 작성 원칙

- **구현 완료**: 코드·테스트·실행 결과로 확인 가능한 사실만 쓴다.
- **설계 완료**: 요구사항·아키텍처·ADR로 합의했지만 아직 코드가 없는 내용이다.
- **구현 예정**: 로드맵에만 있는 작업이며, 자소서에서 완료한 경험처럼 표현하지 않는다.
- 수치(성능, 테스트 수, 비용 절감 등)는 실제 측정값이 생긴 뒤에만 기록한다.

---

## 1. 프로젝트 시작 이유

나는 여행할 때 관광지와 음식점, 숙소를 미리 정하고 1~2시간 단위로 일정을 계획해 표로 정리하는 편이다. 
그러나 장소를 각각 조사한 뒤 이동 동선까지 고려해 효율적인 경로로 배치하려면 시간이 많이 걸린다.
때문에 이러한 일련의 과정을 대신 간편하게 해결할 수 있는 서비스가 있다면 좋겠다는 생각을 많이 했었다.
이에 더해 일정표를 예쁘게 만들어 줄 수 있다면 더욱 좋다고 생각했었다.

Routy는 이 과정을 단순화하기 위해 시작한 AI 여행 일정 관리·경로 최적화 서비스다. 
사용자가 국가 또는 도시, 여행 기간, 반드시 방문할 장소와 음식 선호를 입력하면 도시·장소 후보를 탐색하고, 
선택한 장소를 기준으로 숙소·식사·이동 시간을 고려한 여행 계획을 만드는 것을 목표로 한다.

### 한 줄 소개

**AI의 후보 추천과 서버의 검증 가능한 일정·경로 계산을 분리하고, 단계형 웹 화면으로 여행 계획 수립 과정을 연결하는 Spring Boot 웹 프로젝트**

---

## 2. 현재 상태와 목표 범위

| 구분 | 현재 확인된 상태 | 포트폴리오 표현 방법 |
|---|---|---|
| 애플리케이션 기반 | Spring Boot 실행 골격과 `GET /hello`, Java 21·Gradle, Docker MySQL 구성이 있다. | “Spring Boot 기반 프로젝트와 로컬 개발 기반을 구성했다.” |
| 설계 | 요구사항, API, DB, 테스트, 운영, ADR, 단계별 로드맵을 문서화했다. | “구현 전에 도메인 경계와 외부 의존성·테스트 전략을 설계했다.” |
| 핵심 도메인 | Place, Route, TravelPlan, Recommendation, AI 연동은 구현 전이다. | 완료 경험으로 쓰지 않고 “구현 예정 구조”로만 설명한다. |
| 외부 연동 | Google Places·Routes, OpenAI의 계약과 보안·운영 기준은 설계했고 실제 연동은 구현 전이다. | 실제 연동·비용 절감·장애 처리 경험으로 표현하지 않는다. |
| 프론트엔드 | 참고용 정적 자산은 있으나 Routy 전용 UI의 설계·구현·API 연결은 시작 전이다. | “프론트엔드 완성”이라고 표현하지 않고 S1 검증 후 결과를 기록한다. |

### 목표 사용자 흐름

```text
국가 또는 도시 선택
  → 도시·관광지 후보 탐색
  → 필수 장소·호텔·음식 조건 선택
  → 이동 시간·체류 시간·식사 시간을 고려한 일정 생성
  → 저장된 일정 조회 및 식사 슬롯 기반 음식점 후보 검색
```

---

## 3. 핵심 구현 계획

아래는 구현 예정 기능과 그 기능을 통해 검증하려는 역량이다. 완료 후에는 각 항목에 PR·테스트 결과·문제 해결 사례를 연결한다.

| 구현 영역 | 구현 목표 | 보여줄 수 있는 역량 |
|---|---|---|
| Place | Google Place ID를 내부 `Place` 참조로 관리하고, 국가·도시·관광지·호텔 역할을 검증한다. | 외부 데이터 모델과 내부 영속 모델의 분리, 입력 검증 |
| Route | 좌표 기반 거리 계산, Nearest Neighbor, Google Routes 이동 시간 행렬을 분리한다. | 순수 알고리즘 설계, 결정성, 단위 테스트 |
| TravelPlan | 날짜별 방문 순서·도착/출발 시각·식사 슬롯을 저장하고 교체·삭제한다. | Aggregate, Transaction, JPA 연관관계 |
| Recommendation | 방문 장소·호텔·음식점 후보를 일정과 이동 비용에 따라 평가한다. | 정책 분리, 점수 계산, 정렬 기준 설계 |
| AI | 국가 기반 도시 후보를 구조화된 응답으로 받고 Google Places로 재검증한다. | LLM 출력 검증, 외부 API 장애·계약 관리 |
| 운영·품질 | profile, migration, health check, fake 기반 자동 테스트를 구성한다. | 환경 분리, 보안, 테스트 가능한 설계 |
| Frontend | Vanilla HTML·CSS·JavaScript로 단계형 UI를 새로 설계하고 준비된 API를 순차 연결한다. | 상태 관리, 비동기 오류 처리, 반응형·접근성, 브라우저 검증 |

---

## 4. 설계 이유와 고민 과정

### 4.1 AI와 서버의 책임을 분리한 이유

처음에는 AI에게 “최적의 여행 일정” 전체를 요청하는 방식도 생각할 수 있었다. 하지만 이동 거리, 방문 순서, 일정 수용 가능 여부는 같은 입력에 대해 재현 가능한 결과와 명확한 검증이 필요한 문제다.

그래서 AI는 국가 기반 도시 후보와 추천 이유처럼 자연어·후보 생성에 강한 일을 맡기고, 서버는 Google 검증·거리 계산·경로 최적화·호텔 및 음식점 점수·일정 배치를 맡도록 설계했다.

**면접에서 강조할 점**

- LLM 출력은 유용한 후보일 뿐, 데이터의 사실성이나 최적해를 보장하지 않는다.
- AI 결과는 DTO validation과 Google Places 재검증을 통과한 뒤에만 사용한다.
- 일정 생성 규칙을 서버에 두면 단위 테스트, 오류 추적, 알고리즘 교체가 쉬워진다.

### 4.2 도메인 중심 패키지 구조를 선택한 이유

처음에는 `controller`, `service`, `repository`처럼 기술 계층만으로 패키지를 나누는 방식을 고려했다. 그러나 Routy는 장소, 경로, 일정, 추천, AI처럼 역할이 뚜렷한 기능이 함께 성장한다.

따라서 `place`, `route`, `travelplan`, `recommendation`, `ai`, `user` 도메인 아래에 필요한 계층을 두는 구조를 선택했다. 기능을 수정할 때 관련 코드를 한 도메인 안에서 찾기 쉽고, 다른 도메인의 Repository나 내부 구현을 직접 호출하는 결합을 줄일 수 있다.

### 4.3 Controller · Service · Repository · DTO를 나눈 이유

```text
Controller  : HTTP 요청·응답, 요청 DTO validation
Service     : 유스케이스 조합, 비즈니스 규칙, Transaction 경계
Repository  : Entity 조회·저장
DTO         : API 또는 도메인 간 데이터 전달
Entity      : 영속 상태와 도메인 관계
```

이 분리는 “클래스를 많이 만들기 위해서”가 아니다. HTTP 형식, DB 구조, 외부 API 형식이 각각 바뀌어도 여행 일정 규칙이 함께 흔들리지 않도록 변경 이유를 분리하기 위한 선택이다.

### 4.4 외부 API를 Client 인터페이스와 fake로 분리한 이유

Google Places·Routes와 OpenAI는 네트워크, 비용, rate limit, 제공자 장애라는 불확실성을 가진다. Service가 HTTP 구현을 직접 알면 테스트마다 실제 API를 호출하거나, 제공자 형식 변경이 비즈니스 로직까지 전파될 수 있다.

그래서 `AiClient`, `GooglePlacesClient`, Google Routes client처럼 외부 통신 계약을 분리하고, 자동 테스트에서는 fake 또는 mock을 사용하도록 계획했다. 이 구조에서는 성공·timeout·5xx·잘못된 응답을 비용 없이 재현할 수 있다.

### 4.5 Google Place ID를 내부 참조 기준으로 둔 이유

관광지·호텔·음식점의 이름, 주소, 좌표를 모두 내부 DB에 복제하면 최신성 관리와 제공자 콘텐츠 정책 문제가 생긴다. Routy는 전 세계 장소 데이터 자체를 소유하는 서비스가 아니라, 사용자가 선택한 장소를 기준으로 여행 계획을 만드는 서비스다.

따라서 내부 DB에는 `google_place_id`와 내부 식별자를 가진 가벼운 `Place` 참조를 두고, 장소 표시 정보는 필요한 시점에 외부 제공자에서 조회하는 방향을 선택했다. 같은 장소를 여러 일정에서 재사용하면서도 장소 콘텐츠 전체를 불필요하게 저장하지 않는 구조다.

### 4.6 경로 알고리즘을 순수 Java로 분리한 이유

경로 계산은 Spring Bean, JPA Repository, HTTP Client, AI Client에 의존하지 않는 순수 Java 로직으로 유지한다. 입력으로 거리·시간 행렬을 받고 방문 순서를 반환하게 하면, 외부 API 없이 같은 입력을 반복 테스트할 수 있다.

첫 알고리즘은 복잡도를 통제하기 위해 Nearest Neighbor를 사용하고, 이후 2-opt를 추가해 개선 전후 거리와 결과를 비교할 계획이다. 중요한 것은 처음부터 최적해를 주장하는 것이 아니라, 기준선과 개선 효과를 측정 가능한 형태로 만드는 것이다.

### 4.7 외부 검증 후 트랜잭션을 시작하는 이유

여행 계획 생성에는 Google Places 검증, Google Routes 이동 시간 조회, 일정 계산이 필요하다. 이 과정에서 DB 트랜잭션을 먼저 열면 긴 네트워크 대기 동안 DB 연결을 점유하고, 중간 실패 시 일부 Place나 일정 데이터가 남을 위험이 있다.

그래서 외부 검증과 계산을 먼저 완료하고, 모든 조건이 충족됐을 때만 하나의 트랜잭션으로 Place 참조와 TravelPlan Aggregate를 저장하도록 설계했다.

### 4.8 Flyway migration과 `ddl-auto: validate`를 선택한 이유

JPA의 자동 스키마 변경은 초기 실험에는 빠르지만, 어떤 제약조건이 언제 추가됐는지 추적하기 어렵다. Routy는 첫 스키마부터 Flyway versioned migration으로 관리하고, Hibernate는 스키마 일치 여부만 확인하도록 계획했다.

이를 통해 DB 변경 이력, 빈 DB 재현, UNIQUE·FK·CHECK 제약의 검증을 명시적으로 남기는 것을 목표로 한다.

### 4.9 프론트엔드를 새로 설계하는 이유

현재 정적 리소스에는 참고용 Travela 자산이 있지만, Routy의 국가·도시 선택부터 저장 일정 재조회까지 이어지는 9단계 흐름을 그대로 표현하는 완성 UI는 아니다. 기존 템플릿을 억지로 확장하기보다 `static/Routy` 안에서 Routy 전용 HTML·CSS·JavaScript를 새로 설계할 계획이다.

별도 React 애플리케이션은 현재 프로젝트의 Spring 학습 범위와 빌드·배포 복잡도를 크게 늘리므로 기본안으로 선택하지 않았다. S1-01에서 참고 이미지의 시각적 특징, 디자인 시스템, 상태 모델과 접근성을 먼저 정하고, 각 백엔드 API가 준비되는 시점에 S1-02~S1-07로 나누어 연결한다.

**구현 후 확인할 점**

- 뒤로 이동해도 이전 단계의 입력과 선택이 유지되는가?
- 로딩 중 중복 제출과 오래된 응답의 화면 반영을 막았는가?
- validation·빈 결과·provider 장애를 서로 다른 복구 행동으로 안내하는가?
- 지도 로딩이 실패해도 저장된 시간표를 유지하는가?
- 모바일·데스크톱과 키보드 탐색을 브라우저 테스트로 검증했는가?

---

## 5. AI 사용 항목과 안전장치

### 현재 AI 사용 범위

| 항목 | AI 역할 | 서버 역할 |
|---|---|---|
| 국가 기반 도시 추천 | 도시 이름·국가 코드·추천 이유를 구조화해 제안 | 후보 수·필수 필드·국가 코드 검증, Google Places 재검증, Place ID 부여 |
| 도시 추천 이유 | 사용자가 읽을 수 있는 짧은 이유 생성 | 길이·형식 검증 후 표시 |
| 음식 자연어 해석 | 후속 확장 후보 | 현재 MVP에서는 입력 문자열을 검증해 검색 조건으로 사용 |

### AI에 맡기지 않는 항목

- Google Place ID 생성 또는 추측
- 장소 존재·유형·도시 소속 검증
- 최단 거리 계산과 방문 순서 최적화
- 호텔·음식점의 최종 순위 결정
- DB 저장 여부와 여행 일정의 비즈니스 규칙

### 구현 후 확인할 질문

- 구조화된 AI 응답이 계약을 벗어났을 때 어떤 오류로 변환했는가?
- timeout·5xx는 몇 번 재시도하고, 인증 오류는 왜 재시도하지 않는가?
- AI 후보를 Google Places로 다시 검증하지 않으면 어떤 문제가 생기는가?

---

## 6. 기술 스택과 사용 이유

| 기술 | 상태 | 선택 이유 |
|---|---|---|
| Java 21 | 기반 구성됨 | Spring 생태계와 정적 타입 기반의 유지보수성을 학습하기 위해 선택했다. |
| Spring Boot 4.1.1 | 기반 구성됨 | 웹 요청 처리와 DI 기반의 계층 분리를 학습하기 위한 애플리케이션 기반이다. |
| Gradle | 기반 구성됨 | 의존성·빌드·테스트 실행을 일관되게 관리한다. |
| MySQL 8.4 + Docker Compose | 컨테이너 구성됨 | 로컬에서 동일한 DB 환경을 재현하기 위한 기반이다. |
| Spring Data JPA | 도입 예정 | Entity 관계와 Repository 기반 영속성 접근을 학습하기 위해 도입한다. |
| Flyway | 도입 예정 | DB 변경 이력과 재현 가능한 migration을 관리한다. |
| Google Places / Routes | 연동 예정 | 장소 검증과 정적 이동 거리·시간 데이터를 제공한다. |
| OpenAI API | 연동 예정 | 국가 기반 도시 후보와 추천 이유 생성을 보조한다. |
| JUnit / fake·mock | 확장 예정 | 외부 API 비용·네트워크에 의존하지 않는 자동 테스트를 구성한다. |
| HTML·CSS·JavaScript | 설계 예정 | Spring Boot 정적 리소스 범위에서 Routy 전용 단계형 화면을 구현하고 API 흐름을 학습한다. |

---

## 7. 자소서 문장 초안

### 현재 사실에 기반한 문장

> 여행 계획을 세울 때 장소 탐색과 이동 동선 설계에 반복적으로 시간이 드는 문제를 해결하고자, AI 여행 일정 관리·경로 최적화 서비스 Routy를 기획했습니다. 구현 전 요구사항·API·DB·테스트·운영 기준을 분리해 문서화하고, AI 추천·외부 장소 데이터·서버 비즈니스 규칙의 책임 경계를 설계했습니다.

> 외부 AI의 추천 결과를 일정 생성의 정답으로 사용하지 않고, 후보 생성은 AI가 맡고 장소 검증·경로 계산·일정 배치는 서버가 담당하도록 설계했습니다. 이를 통해 결과의 재현성과 테스트 가능성을 확보하는 방향을 고민했습니다.

### 구현 완료 후 보강할 문장 틀

> Google Places와 Routes 연동에서 [문제 상황]을 해결하기 위해 [설계 또는 구현]을 적용했습니다. 그 결과 [테스트 결과·측정 수치·사용자 영향]을 확인했습니다.

> TravelPlan 생성 과정에서 [외부 검증/저장 실패 사례]를 고려해 [트랜잭션 또는 검증 방식]을 적용했고, [통합 테스트 또는 재현 결과]로 부분 저장이 발생하지 않음을 검증했습니다.

`[ ]` 안의 내용은 구현·테스트·측정 후에만 채운다.

---

## 8. 예상 면접 질문과 답변 초안

### Q. 왜 AI에게 최적의 여행 일정을 전부 맡기지 않았나요?

AI는 도시 후보나 설명처럼 자연어 기반 후보 생성에는 적합하지만, 최단 경로·시간 용량·저장 규칙은 재현성과 검증이 중요합니다. 그래서 AI는 후보 생성, 서버는 검증과 결정적 계산을 맡도록 역할을 나눴습니다. 이 구조라면 AI 응답 품질이 흔들려도 핵심 일정 규칙은 테스트 가능한 코드로 유지할 수 있습니다.

### Q. 왜 도메인 중심 패키지 구조를 택했나요?

Routy는 장소, 경로, 일정, 추천, AI처럼 변화 이유가 다른 기능으로 구성됩니다. 각 도메인 안에 Controller·Service·Repository·DTO를 두면 기능 변경 시 관련 코드를 함께 찾기 쉽고, 다른 도메인의 내부 구현을 직접 참조하는 결합을 줄일 수 있다고 판단했습니다.

### Q. 외부 API를 fake로 분리하면 어떤 장점이 있나요?

자동 테스트가 네트워크·API 키·비용·제공자 상태에 영향을 받지 않습니다. 또한 timeout, 5xx, 잘못된 응답처럼 실제 환경에서 재현하기 어려운 경우를 고정된 입력으로 검증할 수 있습니다. 실제 API 연동은 자동 테스트와 분리한 수동 smoke 환경에서 확인할 계획입니다.

### Q. 왜 외부 호출 전에 DB 트랜잭션을 열지 않나요?

외부 API 호출은 지연되거나 실패할 수 있으므로, 트랜잭션을 오래 유지하면 DB 연결을 불필요하게 점유하고 실패 시 부분 저장 위험이 커집니다. 검증과 계산을 먼저 끝내고, 성공한 결과만 짧은 트랜잭션에서 저장하는 방식이 더 안전하다고 판단했습니다.

### Q. Place 정보를 모두 DB에 저장하지 않는 이유는 무엇인가요?

장소 데이터의 최신성을 직접 관리하는 범위는 프로젝트 목적을 벗어납니다. 또한 외부 제공자 콘텐츠의 저장·표시 정책을 고려해야 합니다. 따라서 내부에서는 Google Place ID를 가진 참조만 관리하고, 표시용 정보는 필요한 때 조회하도록 경계를 정했습니다.

---

## 9. 구현 후 채울 성과 기록

| 날짜 | 작업 ID | 문제 | 선택한 해결 방식 | 검증 결과 | 자소서·면접 키워드 |
|---|---|---|---|---|---|
|  |  |  |  |  |  |

### 기록할 수 있는 근거 예시

- 단위·통합·API 테스트 실행 결과
- migration 적용 결과와 DB 제약 검증 사례
- 외부 API timeout·5xx·잘못된 응답을 fake로 재현한 사례
- Nearest Neighbor와 2-opt의 거리 비교 결과
- 쿼리 수, 처리 시간, 호출 수처럼 실제로 측정한 지표
- 설계 변경 전후의 트레이드오프와 ADR 기록

---

## 10. 참고 기준 문서

- 요구사항과 MVP 범위: `docs/01-requirements.md`
- 아키텍처와 책임 경계: `docs/02-architecture.md`
- DB·API 계약: `docs/03-database.md`, `docs/04-api-spec.md`
- 설계 결정: `docs/06-decisions.md`
- 현재 구현 상태: `docs/07-implementation-readiness.md`
- 테스트·운영·완료 기준: `docs/08-test-strategy.md`, `docs/09-operations.md`, `docs/10-definition-of-done.md`
- 구현 순서: `docs/11-command-roadmap.md`





